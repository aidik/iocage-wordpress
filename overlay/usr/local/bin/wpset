#!/bin/csh

give_message()
{
if [ $? -eq 0 ] ; then
    echo "Making changes"
else
    echo "Error">2 ; exit 1
fi
}

case $1 in
    admin) give_message
           ;;
    wpurl) give_message
           ;;
    *) echo "Unknown option">&2 ; exit 1
           ;;
esac